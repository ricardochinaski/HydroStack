/// Indica si Firebase se inicializó correctamente.
/// Cuando es `false` (por ejemplo en web sin configuración, o si falta
/// google-services.json), la app usa las implementaciones mock.
bool firebaseReady = false;
